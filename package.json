{
  "name": "@mishrab/keychain",
  "version": "0.2.4",
  "description": "macOS Keychain access with Touch ID biometric confirmation",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "flnx-keychain": "dist/cli.js"
  },
  "files": [
    "dist/",
    "native/keychain-helper/Package.swift",
    "native/keychain-helper/Sources/",
    "native/keychain-helper/*.entitlements",
    "scripts/",
    "bin/"
  ],
  "scripts": {
    "build:swift": "bash scripts/build-swift.sh",
    "build:ts": "bun build src/index.ts src/cli.ts --outdir dist --target node",
    "build": "bun run build:swift && bun run build:ts",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "postinstall": "bash scripts/build-swift.sh",
    "prepublishOnly": "bun run build",
    "prepack": "bash scripts/prepack.sh"
  },
  "os": ["darwin"],
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.3.0",
    "@types/bun": "latest"
  }
}
